import React from 'react';
import TextField from '@material-ui/core/TextField';

import { withAdludioTheme } from '../common/theme';

export default withAdludioTheme(props => <TextField {...props} />);
